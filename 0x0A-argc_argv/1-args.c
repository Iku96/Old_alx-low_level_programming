#include <stdio.h>

/**
 * main - Entry Point
 * @argc: arguments count
 * @argv: array pointing to arguments
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%d\n", argc);
	(void)argv;
	return (0);
}
