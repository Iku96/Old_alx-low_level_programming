#include <stdio.h>
#include "main.h"
/**
 * main - prints its name + new line
 * @argc: number of arguments
 * @argv: array pointing to the arguments
 * Return: 0
 */
int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);
	return (0);
}
