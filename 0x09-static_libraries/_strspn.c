#include "main.h"

/*
 * _strspn - computes the length of the initial segment of s which consists
 * entirely of characters in accept
 * @s: the string to search in
 * @accept: the string containing the characters to search for
 *
 * Return: the length of the initial segment of s
 */
unsigned int _strspn(char *s, char *accept)
{

}
