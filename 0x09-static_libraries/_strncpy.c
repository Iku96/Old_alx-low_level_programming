#include "main.h"

/*
 * _strncpy - copies up to n bytes from the src string to the dest string. If
 * the src string is shorter than n bytes, the remaining bytes in dest are
 * filled with null bytes. If src is longer than n bytes, only n bytes are
 * copied
 * @dest: the destination string
 * @src: the source string
 * @n: the number of bytes to copy from src
 *
 * Return: the pointer to dest
 */
char *_strncpy(char *dest, char *src, int n)
{

}
